extends ./layout.pug

block body
    .container.bio
        .row.center-xs  
            .col-xs-10.col-sm-6.about
                != PrismicDOM.RichText.asHtml(document.results[0].data.cv, ctx.linkResolver)
